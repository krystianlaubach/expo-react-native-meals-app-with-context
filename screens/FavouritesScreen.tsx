import { StyleSheet } from 'react-native';

type Props = {

};

export default function FavouritesScreen({}: Props): JSX.Element {
    return (
        <>
        </>
    );
}

const styles = StyleSheet.create({

});
